apiVersion: v1
kind: Service
metadata:
  name: {{ include "samma-app.fullname" . }}
  labels:
    {{- include "samma-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "samma-app.selectorLabels" . | nindent 4 }}
